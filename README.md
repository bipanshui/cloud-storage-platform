# Cloud Storage Platform

Monorepo scaffold for a React frontend and Node.js backend.

## Structure

- `client/` contains the Vite + React frontend.
- `server/` contains the Express-style backend layout.

## Next Steps

1. Install client dependencies in `client/`.
2. Install server dependencies in `server/`.
3. Fill in environment variables before running the backend.

